{
  "name": "vesper-desk-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend service for Vesper Desk. First job: an authenticated download proxy for vesper-desk's GitHub releases.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.0",
    "typescript": "^5.7.3"
  }
}
